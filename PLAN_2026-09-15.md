# 계획 — 2026-09-15

> **파일명의 날짜가 마지막 갱신일이다.** 내용을 고치면 파일명도 같이 바꾼다 (`mv PLAN_2026-09-15.md PLAN_YYYY-MM-DD.md`). 무엇을 왜 바꿨는지는 맨 아래 **갱신 이력**에 한 줄 남긴다.
>
> 이 문서는 **랩 전체의 방향**이다. 보유 장비 기준의 측정 항목과 Phase 0~5 일정은 [`docs/09-research-plan.md`](docs/09-research-plan.md) 에 있고, 여기서 중복하지 않는다.

---

## 0. 한 문장

**Python-like DSL(특히 Gluon)로 sm_120 양자화 커널을 쓰되, layout 을 1급 대상으로 다뤄 변환 비용을 없애는 것.**

---

## 1. 세 갈래와 그것을 잇는 축

| | 갈래 | 지금 상태 |
|---|---|---|
| **A** | **Gluon 기반 컴파일러** — Python-like DSL 조사, TTGIR 진입 지점 | 문서만 있음. 코드 0줄 |
| **B** | **F₂ 논문 스터디** — layout 대수의 수학적 바탕 | 기초 완료(12~14). **논문 자체는 아직 안 읽음** |
| **C** | **LinearLayout × 양자화 효율** — 메인 연구 | 배경 완료(10·11·15). 가설 단계 |

셋을 잇는 축은 **LinearLayout** 하나다.

```
   A Gluon          "layout 을 사람이 직접 선언하는 DSL"
      │
      ├── LinearLayout ───┬── B F₂     "그 선언의 대수적 정체"
      │                   │
      └───────────────────┴── C 양자화  "스케일 오퍼랜드가 그 틀에 안 들어간다"
```

C 가 목적이고, A 는 그것을 건드릴 수 있는 도구, B 는 그것을 이해하는 언어다.

---

## 2. 지금까지 확립된 것 (2026-09-15 기준)

### 문서

| 묶음 | 문서 | 상태 |
|---|---|---|
| 하드웨어 | 00~07 | 1차 출처 기반. `(미확인)` 항목 다수 |
| 스택 | 08 (Helion·Triton·Gluon), 09 (연구 계획) | 완료 |
| 메모리·배치 | 10 (§0.6 = 레지스터 안의 배치) | 완료 |
| LinearLayout | 11 (이력), 12~14 (F₂·전치·소거법), 15 (심화 예제) | 완료 |

### 확립된 사실 (되돌리지 말 것 — 근거는 `CLAUDE.md` 의 **LinearLayout 을 다룰 때** 절)

- `C = A.invertAndCompose(B)` 는 **`B⁻¹ ∘ A`**. `A` = 출발 배치, `B` = 도착 배치.
- 변환 비용은 **`C` 에서 항등이 아닌 채 남는 축**이 정한다 — 없음 / `register`(공짜) / `lane`(`shfl`) / `warp`·`block`(SMEM 왕복). **거리가 아니라 경계다.**
- LinearLayout 은 Blackwell 이전에 들어왔다 (PR #3794, 2024-05). MX 스케일이 방아쇠가 아니다.
- sm_120 은 `tcgen05`/TMEM 이 없어 **A·B·누산기·스케일이 전부 레지스터 파일**에 있다. 스레드당 255개(=1,020 B) 한도.

### 아직 없는 것

- **측정이 0건이다.** 문서의 모든 성능 수치는 벤더 공개값이거나 `(계산값)` 이다.
- Gluon 코드 0줄. 커널 0개.
- LinearLayout 논문 미독 (11 문서는 저장소 이력 기반이지 논문 기반이 아니다).

---

## 3. 핵심 가설 — 왜 양자화에 layout 인가

이번에 PTX 1차 확인으로 나온 사실 하나가 출발점이다.

> **sm_120 의 블록 스케일 선택은 `{byte-id, thread-id}` 즉치 선택자다** — 배치(layout)가 아니다. `thread-id-a` 가 quad 안의 스레드 쌍을 고르고, `byte-id` 가 32-bit 레지스터 **안의 바이트**를 고른다. (PTX ISA §9.7.16.3 Table 46)

여기서 두 가설이 나온다. **둘 다 아직 가설이고, 검증 전이다.**

| | 가설 | 틀렸다면 |
|---|---|---|
| **H1** | 스케일 오퍼랜드는 LinearLayout 프레임 **밖**에 있어서, A/B fragment 와의 정렬이 어긋나도 컴파일러가 `convert_layout` 으로 인지하지 못하고 재배치가 코드에 남는다 | C 의 전제가 무너진다 → 레지스터 압박(H2) 쪽으로 방향 전환 |
| **H2** | sm_120 은 스케일까지 RF 에 있어 255개 한도가 타일 크기를 먼저 막고, 그것이 공개 NVFP4 커널이 FP4 [dense] peak 의 약 60% 에 머무는(03 §3.5, Colfax 튜토리얼 [2차 출처]) 원인 중 하나다 | 원인이 다른 데 있다 → Phase 0 측정으로 재탐색 |

**H1 이 맞다면 연구 질문이 이렇게 선다:**
*블록 스케일의 배정을 LinearLayout 으로 표현할 수 있는가? 표현할 수 있다면 스케일↔fragment 변환이 `C` 에서 어느 등급으로 떨어지고, 그것을 `register` 등급까지 끌어내릴 수 있는가?*

---

## 4. 갈래별 계획

### A. Gluon 기반 컴파일러

| # | 할 일 | 산출물 | 선행 |
|---|---|---|---|
| A0 | 환경 고정 — Triton/Gluon 커밋 핀, sm_120 빌드 확인 | 버전표 (`docs/07` 갱신) | — |
| A1 | Gluon 으로 `mma.sync` 하나 돌리기 (MXFP8) | 최소 커널 + 나가는 PTX | A0 |
| A2 | layout 을 **명시 선언**했을 때 `convert_layout` 이 정말 0개인지 확인 | TTGIR/PTX diff | A1 |
| A3 | `AutoLayout` 이 무엇을 고르는지 관찰 (`--gluon-resolve-auto-encodings`) | 관찰 노트 | A2 |
| A4 | 같은 커널의 Triton 판과 비교 — 나가는 명령 차이 | 비교표 | A3 |

**A2 가 이 갈래의 핵심이다.** 11 문서 §6-2 에서 "비용 0 을 사용자가 선언하면 컴파일러가 검사해 준다"고 적은 것이 실제로 그런지를 눈으로 보는 단계다.

### B. F₂ 논문 스터디

| # | 할 일 | 산출물 | 선행 |
|---|---|---|---|
| B0 | LinearLayout 논문 정독 — 저장소 이력에서 얻은 그림과 **어긋나는 곳**을 찾는다 | `docs/16-linearlayout-paper.md` (가칭) | — |
| B1 | `examples/` 의 F₂ 조각들을 하나의 모듈로 정리 (기저·역행렬·합성·등급 판정) | `examples/f2lib.py` | — |
| B2 | CuTe 의 layout algebra 와 비교 — 같은 문제를 다르게 푼 사례 | `docs/17-cute-vs-ll.md` (가칭) | B0 |

B0 의 목적은 요약이 아니라 **대조**다. 11 문서는 의도적으로 논문을 안 보고 커밋만 읽어 쓴 것이라, 두 그림이 어디서 갈리는지가 그 자체로 결과다.

### C. LinearLayout × 양자화 효율 (메인)

| # | 할 일 | 산출물 | 선행 |
|---|---|---|---|
| C0 | **스케일 배치의 실제 모양 확정** — PTX Table 46 + Triton `tl.dot_scaled` lowering 을 나란히 놓고, 스케일이 IR 에서 무엇으로 표현되는지 | `docs/18-scale-layout.md` (가칭) | — |
| C1 | H1 검증 — 스케일↔fragment 정렬이 어긋난 케이스를 만들고 나가는 코드 확인 | 재현 커널 + PTX/SASS | C0, A1 |
| C2 | 비용 측정 — `ncu` 로 변환에 쓰이는 명령·SMEM 왕복·배리어 수 | 측정표 | C1 |
| C3 | 개선안 — 스케일 배치를 LinearLayout 으로 기술했을 때 등급이 내려가는가 | 패치 또는 제안서 | C2 |
| C4 | H2 검증 — 타일 크기 스윕에서 레지스터 한도가 먼저 막는지 | occupancy/스필 표 | Phase 0 |

**C0 이 먼저다.** 지금은 "스케일은 layout 이 아니다"까지만 확인했고, **Triton 이 그것을 IR 에서 어떻게 들고 있는지는 모른다.** 그걸 모르는 채로는 H1 을 검증할 수 없다.

---

## 5. 순서 — 의존 관계

```
   A0 환경 고정
      │
      ├──▶ A1 최소 커널 ──▶ A2 layout 선언 검사 ──▶ A3/A4
      │         │
      │         ▼
   C0 스케일 표현 확정 ──▶ C1 H1 검증 ──▶ C2 측정 ──▶ C3 개선안
      │
   B0 논문 정독 (병렬, 언제든)
      │
   Phase 0 (09 문서) ──▶ C4 H2 검증
```

- **A0 → C0 → C1 이 임계 경로다.**
- B0 는 아무 때나 끼울 수 있고, C1 이 막히면 B0 가 실마리를 줄 가능성이 높다.
- Phase 0 측정(09 문서)은 C4 이전에만 끝나면 된다.

---

## 6. 측정·보고 규칙

[`docs/09-research-plan.md`](docs/09-research-plan.md) §5 를 그대로 따른다. layout 연구에만 추가되는 것:

- **주장마다 나가는 코드를 첨부한다.** "변환이 사라졌다"는 TTGIR/PTX/SASS 없이 쓰지 않는다.
- `convert_layout` 을 셀 때는 **op 개수가 아니라 나가는 명령**을 센다 — op 은 남아도 명령이 0개일 수 있다(`register` 등급).
- Triton/Gluon 커밋을 **항상 적는다.** 이 프로젝트는 빠르게 바뀌어서 동작이 커밋마다 다르다.
- 예제 숫자는 `docs/examples/` 에 실행 가능한 스크립트로 남긴다.

---

## 7. 막힐 만한 곳

| 위험 | 징후 | 대응 |
|---|---|---|
| sm_120 라이브러리 지원이 늦다 | FA4·DeepGEMM 이 sm_100 전용 | 처음부터 직접 작성 전제. 비교 기준은 cuBLASLt |
| Gluon API 가 빠르게 바뀐다 | 릴리스마다 시그니처 변경 | 커밋 핀. 버전표를 A0 산출물로 명시 |
| **H1 이 틀린다** | 스케일이 이미 layout 으로 표현돼 있다 | C 를 H2(레지스터 압박) 쪽으로 전환. C4 를 앞당긴다 |
| 배경 문서만 늘어난다 | 코드 0줄인 채로 문서가 16, 17, 18… | **A1(최소 커널)을 다른 무엇보다 먼저 끝낸다** |

마지막 줄이 지금 가장 큰 위험이다. 문서 16개에 커널 0개다.

---

## 8. 바로 다음에 할 일

1. **A0** — Triton/Gluon 을 sm_120 에서 빌드하고 커밋을 핀한다. (반나절)
2. **A1** — Gluon 으로 MXFP8 `mma.sync` 한 개를 돌리고 PTX 를 뜬다. (1~2일)
3. **C0** — `tl.dot_scaled` 의 lowering 에서 스케일이 무엇으로 표현되는지 확인한다. (클론 + grep, 반나절)

셋 다 끝나면 이 문서를 갱신하고 파일명 날짜를 바꾼다.

---

## 갱신 이력

| 날짜 | 바뀐 것 |
|---|---|
| 2026-09-15 | 최초 작성. 세 갈래(A Gluon / B F₂ / C 양자화) 정의, H1·H2 가설 수립, A0→C0→C1 임계 경로 확정 |
